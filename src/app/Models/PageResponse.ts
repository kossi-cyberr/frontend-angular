/** Réponse paginée standard du backend (Dto/PageResponse.java). */
export interface PageResponse<T> {
  content: T[];
  page: number;
  size: number;
  totalElements: number;
  totalPages: number;
  last: boolean;
}
